/**
 * Lexical node classes, commands, and utility functions.
 *
 * Import from '@typix-editor/core/lexical' for direct Lexical access.
 * Most users won't need these — the TypixEditor API covers common operations.
 */
export {
  $createHeadingNode,
  $createParagraphNode,
  $getSelection,
  $isRangeSelection,
  $setBlocksType,
  AutoLinkNode,
  CodeHighlightNode,
  CodeNode,
  FORMAT_TEXT_COMMAND,
  HashtagNode,
  HeadingNode,
  LinkNode,
  ListItemNode,
  ListNode,
  OverflowNode,
  ParagraphNode,
  QuoteNode,
  TableCellNode,
  TableNode,
  TableRowNode,
  TextNode,
} from "./lib";
