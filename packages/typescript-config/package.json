{
  "name": "@typix-editor/typescript-config",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  }
}
