registry=https://registry.npmjs.org/
# The auth-token line for `pnpm publish` is written into ~/.npmrc by the
# release workflow (.github/workflows/release.yml) before the publish step,
# not here — pnpm warns on every install if ${NPM_TOKEN} is unset locally.
