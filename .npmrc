registry=https://registry.npmjs.org/
# Tells pnpm to substitute the NPM_TOKEN env var for registry auth during
# `pnpm publish` in CI. Empty value locally is fine — read-only operations
# (install, fetch) don't require auth on the public registry.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
